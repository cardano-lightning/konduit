openapi: 3.1.0
info:
  title: Cardano Connector Server API
  version: 0.1.0
  description: |
    Minimal HTTP facade over Blockfrost and Koios used by the Cardano connector client.

    The API is read-only except for transaction submission. Amounts are encoded as decimal strings
    to avoid integer precision issues in JavaScript clients.
servers:
  - url: http://localhost:8787
    description: Local development server
tags:
  - name: system
  - name: addresses
  - name: transactions
paths:
  /health:
    get:
      tags: [system]
      summary: Health check
      operationId: getHealth
      responses:
        '200':
          description: Service is reachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        default:
          $ref: '#/components/responses/UnexpectedError'

  /network:
    get:
      tags: [system]
      summary: Active Cardano network
      operationId: getNetwork
      responses:
        '200':
          description: Network inferred from the configured Blockfrost project id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkResponse'
        default:
          $ref: '#/components/responses/UnexpectedError'

  /balance/{address}:
    get:
      tags: [addresses]
      summary: Address balance
      operationId: getBalance
      parameters:
        - $ref: '#/components/parameters/Address'
      responses:
        '200':
          description: Lovelace balance for the address
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalanceResponse'
        default:
          $ref: '#/components/responses/UnexpectedError'

  /utxos_at/{address}:
    get:
      tags: [addresses]
      summary: UTxOs at address
      operationId: getUtxosAt
      parameters:
        - $ref: '#/components/parameters/Address'
      responses:
        '200':
          description: Current UTxOs found at the address
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Utxo'
        default:
          $ref: '#/components/responses/UnexpectedError'

  /transactions/{address}:
    get:
      tags: [transactions]
      summary: Transaction summaries for an address
      operationId: getTransactions
      parameters:
        - $ref: '#/components/parameters/Address'
      responses:
        '200':
          description: Transactions touching the address
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TransactionSummary'
        default:
          $ref: '#/components/responses/UnexpectedError'

  /transaction/{id}:
    get:
      tags: [transactions]
      summary: Transaction summary by id
      operationId: getTransaction
      parameters:
        - $ref: '#/components/parameters/TransactionId'
      responses:
        '200':
          description: Expanded transaction summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionSummary'
        default:
          $ref: '#/components/responses/UnexpectedError'

  /submit:
    post:
      tags: [transactions]
      summary: Submit a signed transaction
      operationId: submitTransaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitRequest'
      responses:
        '200':
          description: Transaction accepted by Koios submit endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitResponse'
        default:
          $ref: '#/components/responses/UnexpectedError'

components:
  parameters:
    Address:
      name: address
      in: path
      required: true
      description: Shelley address encoded as bech32
      schema:
        type: string
        minLength: 1
    TransactionId:
      name: id
      in: path
      required: true
      description: 32-byte transaction hash encoded as lowercase hexadecimal
      schema:
        type: string
        pattern: '^[0-9a-f]{64}$'

  responses:
    UnexpectedError:
      description: Upstream failure or malformed request
      content:
        text/plain:
          schema:
            type: string
            example: unexpected error

  schemas:
    HealthResponse:
      type: object
      required: [status]
      properties:
        status:
          type: string
          const: ok

    NetworkResponse:
      type: object
      required: [network]
      properties:
        network:
          type: string
          enum: [mainnet, preprod, preview]

    BalanceResponse:
      type: object
      required: [lovelace]
      properties:
        lovelace:
          type: string
          pattern: '^[0-9]+$'
          description: Lovelace amount encoded as a decimal string

    AssetObject:
      type: object
      required: [unit, quantity]
      properties:
        unit:
          type: string
          description: Asset identifier, or `lovelace` for Ada
        quantity:
          type: string
          pattern: '^[0-9]+$'
          description: Asset quantity encoded as a decimal string

    TxOutput:
      type: object
      required: [address, value]
      properties:
        address:
          type: string
        value:
          type: array
          items:
            $ref: '#/components/schemas/AssetObject'
        datum_hash:
          type: string
          pattern: '^[0-9a-f]{64}$'
        datum_inline:
          type: string
          description: Inline datum CBOR encoded as hexadecimal
        reference_script_hash:
          type: string
          pattern: '^[0-9a-f]{56}$'

    TxInput:
      allOf:
        - type: object
          required: [transaction_id, output_index]
          properties:
            transaction_id:
              type: string
              pattern: '^[0-9a-f]{64}$'
            output_index:
              type: integer
              minimum: 0
        - $ref: '#/components/schemas/TxOutput'

    Utxo:
      allOf:
        - type: object
          required: [transaction_id, output_index]
          properties:
            transaction_id:
              type: string
              pattern: '^[0-9a-f]{64}$'
            output_index:
              type: integer
              minimum: 0
            reference_script_version:
              type: integer
              minimum: 0
              maximum: 255
            reference_script:
              type: string
              description: Reference script CBOR encoded as hexadecimal
        - $ref: '#/components/schemas/TxOutput'

    TransactionSummary:
      type: object
      required: [id, index, depth, timestamp, inputs, outputs]
      properties:
        id:
          type: string
          pattern: '^[0-9a-f]{64}$'
        index:
          type: integer
          minimum: 0
        depth:
          type: integer
          minimum: 0
        timestamp:
          type: integer
          minimum: 0
          description: Block timestamp in Unix seconds
        invalid_before:
          anyOf:
            - type: integer
              minimum: 0
            - type: string
          description: Optional lower slot bound as returned by upstream
        invalid_after:
          anyOf:
            - type: integer
              minimum: 0
            - type: string
          description: Optional upper slot bound as returned by upstream
        inputs:
          type: array
          items:
            $ref: '#/components/schemas/TxInput'
        outputs:
          type: array
          items:
            $ref: '#/components/schemas/TxOutput'

    SubmitRequest:
      type: object
      required: [transaction]
      properties:
        transaction:
          type: string
          description: Signed transaction CBOR encoded as hexadecimal

    SubmitResponse:
      type: object
      required: [transaction_id]
      properties:
        transaction_id:
          oneOf:
            - type: string
              pattern: '^[0-9a-f]{64}$'
            - type: array
              items:
                type: string
              description: Koios may return an array payload depending on upstream behavior
